# Plugins Python

Un code pour récupérer les articles d'un site web via une URL

## Installation

Pour installer les dépendances nécessaires, exécutez les commandes suivantes :

```bash
python -m pip install requests
pip install beautifulsoup4
```

## Configuration

Vous aurez besoin de modifier les "chemins" vers les articles ainsi que leurs éléments
